# Text stimuli: see `web/data/`

The program-note text corpus (the `<symphony>_processed.csv` stimulus files)
lives in [`neurosymphony/web/data/`](../../../web/data/) — see that
folder's `README.md` for the row layout.

The corresponding valence/arousal ratings are in
[`../ratings/`](../ratings/).
